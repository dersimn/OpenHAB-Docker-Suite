Strategies {
	everyHour 	: "0 0 * * * ?"
	everyDay 	: "0 0 0 * * ?"
	everyMinute : "0 * * * * ?"
	every10mins : "0 */10 * * * ?"

	// if no strategy is specified for an item entry below, the default list will be used
	default = everyUpdate
}

Items {
	Database*
	Heating*
	Temperature*
	Security*
	Presence*
	Weather*
}